type User @entity {
  id: ID!
  address: Bytes!
  deposited: Boolean!
  kyced: Boolean!
  referrals: [Referral!]
  totalBountyWithdrawn: Int!
  totalBountyToWithdraw: Int!
  totalReferralsCount: Int!
}

type Referral @entity {
  id: ID!
  referrer: User!
  referred: User!
}